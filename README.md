# Команды для сборки и запуска проекта
1. Команда для сборки проекта, при изменениях надо запускать снова.
```
docker build -t my-nginx-app .
```
2. Запуск проекта, после успешного запуска можно переходить по **http://localhost:8080/**
```
docker run -d --name nginx-app -p 8080:80 my-nginx-app
```
